---
description: make a small fix to an obvious issue
agent: quickfix
subtask: true
---

There is a relatively obvious issue in the file @$1 around line `$2`.

You don't need more context than the code itself and the LSP information (if available).

Fix the issue with a focused edit.
