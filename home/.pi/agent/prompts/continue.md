---
description: continue user's implementation
argument-hint: "<file> <line> <col>"
---

The user has started implementing a change in the file @$1.

Their cursor is currently on line `$2`, column `$3`.

Continue where they left off, doing your best to align with their intention.

Work from the code itself; read only the surrounding context you actually need.
